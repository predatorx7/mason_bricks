import 'dart:async';

import '{{name.snakeCase()}}_service.dart';

class {{name.pascalCase()}}ServiceImpl implements {{name.pascalCase()}}Service {
  const {{name.pascalCase()}}ServiceImpl();

  {{#methods}}
  @override
  FutureOr<{{{type}}}> {{name.camelCase()}}() {
    throw UnimplementedError();
  }
  {{/methods}}
}
